# 调整推理深度

`（思考模式）` 变体支持三种推理深度级别：

- `low`：最轻量的推理、最快的回答、最低的费用
- `high`：更短的推理链 —— DeepSeek 自身的默认级别
- `max`：最深度的推理、较慢的响应、更高的 token 消耗

你可以在设置中通过 `deepseek-pilot.reasoningEffort` 设置默认值。

当在模型选择器中选中思考模式模型时，模型配置界面也允许你为该特定模型切换推理深度。
